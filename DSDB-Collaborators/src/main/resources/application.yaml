spring:
  data:
    neo4j:
      username: neo4j
      password: test1234
      uri: bolt://localhost

  application:
    name: DSDB-Collaborators

server:
  port: 8085

eureka:
  client:
    register-with-eureka: true
    fetch-registry: false
    service-url:
      defaultZone: http://localhost:8761/eureka
